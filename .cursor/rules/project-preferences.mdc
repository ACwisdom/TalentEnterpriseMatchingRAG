---
description: 本仓库专属约定（在此增删，供 Cursor 自动参考）
alwaysApply: true
---

# 项目专属约定

**你要改的内容就写在本文件中**（在「我的约定」标题下追加列表或段落即可）。保存后，在本仓库里与 AI 对话时会作为项目规则加载。

## 我的约定

（在下面写你的习惯，例如：）

- 与用户沟通默认使用简体中文。
- 未经说明不要修改 `data/` 下业务数据与向量目录中的用户数据。
- 只改前端展示文案时，优先只动 `web/static/index.html`。
- `tests/test_hybrid_retrieval.py` 中带向量索引的用例默认跳过；需本地验证混合检索时设置环境变量 `RUN_HYBRID_INTEGRATION=1` 再跑 pytest。
