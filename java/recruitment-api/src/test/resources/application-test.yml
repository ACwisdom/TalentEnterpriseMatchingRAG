spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:recruitment_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

service:
  api-key: test-api-key
